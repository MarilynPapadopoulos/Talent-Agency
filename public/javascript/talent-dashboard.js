document.querySelector("#modify-button").addEventListener("click", (event) => {
	// need logic here to pass the user ID into the route
	// need session for this
	// document.location.replace(`/update${id}`);
	document.location.replace("/talent/update");
});
